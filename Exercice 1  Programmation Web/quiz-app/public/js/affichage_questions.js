function verifierReponse(choix) {
    const reponseCorrecte = 2; // Choisir la réponse 2 comme correct
    if (choix === reponseCorrecte) {
        alert("Bonne réponse !");
    } else {
        alert("Mauvaise réponse !");
    }
}
